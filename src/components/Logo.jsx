// Знак бренда: уровень, поднявшийся до отметки. Квадратный резервуар, нижняя
// часть залита акцентом, а сама отметка выходит за края — так читается «замер»,
// а не просто заполненный квадрат. Прилив без волны и без буквенной монограммы:
// волна была клише, монограмма ничего не значила, шкала с засечками не читалась.
// Три элемента на весь знак, поэтому живёт и в 16px фавиконе, и в аватарке бота.
export default function Logo({ size = 26, className = '' }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 34 34"
      fill="none"
      aria-hidden="true"
      className={className}
    >
      {/* залитая часть — то, что уже «набралось» */}
      <path d="M6 19.5 H28 V28 H6 Z" fill="var(--brand)" />
      {/* резервуар */}
      <rect x="6" y="6" width="22" height="22" stroke="currentColor" strokeWidth="2.6" />
      {/* отметка уровня, выступает за края */}
      <path d="M2 19.5 H32" stroke="var(--brand)" strokeWidth="3" />
    </svg>
  )
}
