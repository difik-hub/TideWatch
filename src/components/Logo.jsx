// Знак бренда: одна линия, в которой прилив переходит в биржевой график.
// Левая половина — спокойная волна (имя), правая ломается в свечной ход (продукт).
// Так знак говорит и про Tide, и про Watch, оставаясь одним росчерком.
//
// Отвергнутые попытки, чтобы не ходить по кругу: волна сама по себе (носит и
// погодное приложение), монограмма TW (ничего не значила), шкала с засечками
// (не читалась), уровень в резервуаре (выглядел ведром), три столбца (знак
// любого финансового сервиса, без нас).
export default function Logo({ size = 26, className = '' }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 34 34"
      fill="none"
      aria-hidden="true"
      className={className}
    >
      {/* прилив */}
      <path
        d="M3 22c2.5 0 2.5-6 5-6s2.5 6 5 6 2.5-6 5-6"
        stroke="currentColor"
        strokeWidth="2.6"
        strokeLinecap="round"
      />
      {/* и тот же ход, сорвавшийся в рынок */}
      <path
        d="M18 16l4.5 6 4-9 4.5 12"
        stroke="var(--brand)"
        strokeWidth="2.6"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  )
}
